version: '3.8'

services:
  mon-app:
    build: .
    container_name: MediaNet
    restart: unless-stopped
    
    deploy:
      resources:
        limits:
          cpus: '0.5'
          memory: 512M
    
    env_file:
      - .env
    
    volumes:
      - ./data:/app/data